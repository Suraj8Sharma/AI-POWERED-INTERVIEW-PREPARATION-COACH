# PrepLoom production requirements — for the Docker image (web/api.py only).
#
# This is a curated subset of requirements.txt: only what web/api.py and the
# AI_BACKEND modules it imports actually need at runtime. It deliberately
# excludes dev/notebook-only tooling (streamlit, jupyter, ipykernel, the
# frontend_interface/ Streamlit prototype's deps, etc.) to keep the image small.
#
# torch (openai-whisper's hard dependency) is installed separately in the
# Dockerfile from PyTorch's CPU-only index — see Dockerfile comment. Do not
# add it here: mixing --extra-index-url into this file lets pip resolve
# unrelated packages against the PyTorch index too, which caused hash
# mismatches during image builds.

# Web framework
fastapi==0.135.3
uvicorn==0.42.0
python-multipart==0.0.24
python-dotenv==1.2.2
pydantic==2.12.5
email-validator==2.3.0

# Auth / DB / storage
supabase==2.10.0

# RAG (question retrieval)
chromadb==1.5.5
langchain-community==0.4.1
langchain-huggingface==1.2.1

# Technical scoring (LLM via HuggingFace Inference)
# (uses langchain-huggingface above)

# Speech-to-text
openai-whisper==20250625
scipy==1.17.1

# Body-language / pose analysis
mediapipe==0.10.35
opencv-contrib-python==5.0.0.93

# Image handling + PDF report generation
numpy==2.4.2
pillow==12.1.1
reportlab==4.2.5

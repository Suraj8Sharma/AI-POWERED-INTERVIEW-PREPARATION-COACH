# PrepLoom Production Requirements (Optimized for Render)
# Remove heavy/desktop-only dependencies

# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# API & HTTP
httpx==0.25.2
python-multipart==0.0.6
aiofiles==23.2.1

# Database & Auth
pymongo==4.6.0
supabase==1.0.3
python-jose==3.3.0
passlib==1.7.4
python-dotenv==1.0.0

# LLM & NLP
langchain==0.0.354
langchain-community==0.1.0
langchain-openai==0.0.7
openai==1.3.9
huggingface-hub==0.19.4
sentence-transformers==2.2.2

# Vector DB
chromadb==0.4.10

# Data Processing
numpy==1.24.3
pandas==2.1.3

# Audio/Speech (minimal)
# Removed: pyttsx3 (desktop-only)
# Removed: librosa (heavy dependencies)

# Optional: Lightweight monitoring
sentry-sdk==1.38.0

# For development only (remove in production if needed)
python-json-logger==2.0.7
